_include:

   xgc-reborn_library:
    
      recipes:
         - inspect.yml
          
      steps:
         - inspect.yml
         - imaging.yml
         - flagging.yml
         - selfcal.yml
         - prepdata.yml
         - calibration_casa.yml
         - calibration_quartical.yml
           
      params:
         - imaging.yml
         - selfcal.yml
         - flagdata.yml
         - information.yml
         - manipulation.yml
         - visualization.yml
         - calibration_casa.yml
         - calibration_quartical.yml
           
   (cultcargo):
      - bdsf.yml
      - smops.yml
      - cubical.yml
      - msutils.yml
      - aimfast.yml
      - shadems.yml
      - rfinder.yml
      - wsclean.yml
      - quartical.yml
      - breizorro.yml
      - tricolour.yml
      - crystalball.yml
       
      - casa:
         - flag.yml
         - setjy.yml
         - polcal.yml
         - plotms.yml
         - listobs.yml
         - bandpass.yml
         - plotants.yml
         - clearcal.yml 
         - calibration.yml
         - mstransform.yml
      
cabs:

   mkdir:
      command: mkdir
      inputs:
         dtype: str
         policies:
            positional: true  
                 
   move:
      command: mv
      policies:
         positional: true
      inputs:
         src:
            dtype: File
            required: true
      outputs:  
         dest:
            dtype: Union[File, Directory]
            required: true
        
   copy:
      command: cp -a
      policies:
         positional: true
      inputs:
         src:
            dtype: File
            required: true
      outputs:
         dest:
            dtype: Union[File, Directory]
            required: true
            
   casa.immath:
      command: immath
      flavour: casa-task
      inputs:
         _include: xgc-reborn_cabs/CASA/immath.yml
   
   casa.rmtables:
      command: rmtables
      flavour: casa-task
      inputs:
         _include: xgc-reborn_cabs/CASA/rmtables.yml
 
   prepkat:
      command: katflip
      backend:
         select: native
         native:
            virtual_env: /home/otieno/py310env
      inputs:
         ms_path: 
            dtype: Directory 
            required: true 
            policies:
               positional: true
         column: 
            dtype: 'str'
            
   quartical:
      image:
         version: 0.2.5-cc0.2.1
         
   qcalsummary:
      command: goquartical-summary
      image:
         name: quartical
         version: 0.2.5-cc0.1.3 
      policies:
         positional: true
         prefix: '--'
         replace:
            '_': '-'
      inputs:
         ms_path:
            dtype: Directory
            must_exist: true
                 
   qcalrestore:
      command: goquartical-restore
      image:
         name: quartical
         version: 0.2.5-cc0.1.3 
      policies:
         positional: true
         prefix: '--'
         replace:
            '_': '-'
      inputs:
         ms_path:
            dtype: Directory
            must_exist: true
            
         zarr_dir:
            dtype: Directory
            mkdir: true
            must_exist: false
            
         column:
            dtype: str
            
         label:
            dtype: str
                    
   qcalbackup:
      command: goquartical-backup
      image:
         name: quartical
         version: 0.2.5-cc0.1.3 
      policies:
         positional: true
         prefix: '--'
         replace:
            '_': '-'
      inputs:
         ms_path:
            dtype: Directory
            must_exist: true
            
         zarr_dir:
            dtype: Directory
            mkdir: true
            must_exist: false
            
         column:
            dtype: str
            
         label:
            dtype: str
            
         nthread:
            dtype: int
            
         field-id:
            dtype: int
        
   qcalplotter:
      name: quartical-plotter
      image:
         version: 0.2.5-cc0.1.3 
         name: quartical
      command: goquartical-plot
      info: Rudimentary plotter for QuartiCal gain solutions
      policies:
         prefix: '--'
      inputs:
         _include: xgc-reborn_cabs/quartical/quartical-plotter.yml
         
   kg2DGa:
      name: kg2GDa
      flavour: python-code
      image:  
         name: quartical
         version: 0.2.5-cc0.1.3
      command: |
         import numpy as np
         import dask
         import dask.array as da
         from daskms.experimental.zarr import xds_to_zarr, xds_from_zarr

         G_table = '::'.join(sourceG.rsplit('/', 1))
         K_table = '::'.join(sourceK.rsplit('/', 1))
         G = xds_from_zarr(G_table)
         K = xds_from_zarr(K_table)
         Ga = []
         D = []
         for g, k in zip(G,K):
            assert g.SCAN_NUMBER == k.SCAN_NUMBER
            assert g.FIELD_ID == k.FIELD_ID
            assert g.DATA_DESC_ID == k.DATA_DESC_ID 
            amplitudes = da.abs(g.gains.data)
            phases = da.angle(g.gains.data)
            ga = g.assign_coords({'param_freq': (('param_freq',), g.gain_freq.values),
                                'param_time': (('param_time',), g.gain_time.values),
                                'param_name': (('param_name',), np.array(['amplitude_XX', 'amplitude_YY'], dtype='<U12'))})
            ga = ga.assign({'gains': (g.gains.dims, amplitudes.astype(g.gains.data.dtype)),
                          'params': (('param_time', 'param_freq', 'antenna', 'direction', 'param_name'), da.take(amplitudes, [0, -1], axis=-1)),
                          'param_flags': (('param_time', 'param_freq', 'antenna', 'direction'), g.gain_flags.data)})
            par_spec = g.GAIN_SPEC
            ga = ga.assign_attrs({'TYPE': 'amplitude',
                                'PARAM_AXES': ['param_time', 'param_freq', 'antenna', 'direction', 'param_name'],
                                'PARAM_SPEC': par_spec})
            Ga.append(ga)

            delay = k.params.data
            off_xx = phases[:, :, :, :, 0]
            off_yy = phases[:, :, :, :, -1]
            del_xx = delay[:, :, :, :, 0]
            del_yy = delay[:, :, :, :, -1]
            params = da.stack((off_xx, del_xx, off_yy, del_yy), axis=-1)
            gains = k.gains.data * da.exp(1.0j*phases)
            pnames = np.array(['phase_offset_XX', 'delay_XX', 'phase_offset_YY', 'delay_YY'], dtype='<U15')
            pfreq = k.param_freq.values
            ptime = k.param_time.values
            # drop params to avoid conflicts
            d = k.drop_vars(['params', 'param_flags'])
            d = d.drop_dims(['param_freq', 'param_name', 'param_time'])
            d = d.assign_coords({'param_freq': (('param_freq',), k.param_freq.values),
                               'param_time': (('param_time',), k.param_time.values),
                               'param_name': (('param_name',), pnames)})
            d = d.assign({'gains': (k.gains.dims, gains),
                        'params': (('param_time', 'param_freq', 'antenna', 'direction', 'param_name'), params),
                        'param_flags': (('param_time', 'param_freq', 'antenna', 'direction'), k.param_flags.data)})  # Need to or flags?
            par_spec = k.PARAM_SPEC
            par_spec[-1] = [4]  # 2 correlations -> 4 parameters
            d = d.assign_attrs({'TYPE': 'delay_and_offset',
                              'PARAM_AXES': ['param_time', 'param_freq', 'antenna', 'direction', 'param_name'],
                              'PARAM_SPEC': par_spec})
            D.append(d)

         for k, g, d, ga in zip(K, G, D, Ga):
            assert g.SCAN_NUMBER == k.SCAN_NUMBER
            assert g.FIELD_ID == k.FIELD_ID
            assert g.DATA_DESC_ID == k.DATA_DESC_ID 
            gains1 = g.gains.values * k.gains.values
            gains2 = ga.gains.values * d.gains.values
            assert np.allclose(gains1, gains2)

            Ga_table = G_table.rstrip('G') + 'Ga'
            D_table = K_table.rstrip('K') + 'D'    
            print(f'Writing amplitudes to {Ga_table}')
            writes = []
            writes.append(xds_to_zarr(Ga, Ga_table))
            print(f'Writing delay_and_offtset to {D_table}')
            writes.append(xds_to_zarr(D, D_table))
            dask.compute(writes)
            print('Done')

      inputs:
         sourceG:
            info: The location to fetch G term from
            dtype: URI
            required: true
         sourceK:
            info: The location to fetch K term from
            dtype: URI
            required: true

      outputs:
         {} 
         
   G2abs:
      name: G2abs
      flavour: python-code
      image:  
         name: quartical
         version: 0.2.5-cc0.1.3
      command: |
         import dask
         import dask.array as da
         from daskms.experimental.zarr import xds_to_zarr, xds_from_zarr
         gain_table = '::'.join(source.rsplit('/', 1))
         G = xds_from_zarr(gain_table)
         for g in G:
           g['gains'] = (g.gains.dims, da.abs(g.gains.data))
         abs_gain_table = gain_table.rstrip('G') + 'Ga'
         print(f'Writing to {abs_gain_table}')
         writes = xds_to_zarr(G, abs_gain_table)
         dask.compute(writes)
         print('Done')

      inputs:
         source:
            info: The location to fetch G term from
            dtype: URI
            required: true

      outputs:
         {}
                     
   casa.setjy:
      flavour: 
         kind: python-code
         input_dict: args
      name: casa.setjy
      command: |
         from omegaconf import OmegaConf
         from casatasks import setjy
         import numpy as np
         if field == 'J0521+1638':
            print(f"Setting manual flux for Polarization calibrator {field} using Manual")
            setjy(
               fluxdensity= [8.33843,0.0,0.0,0.0], 
               spix= [-0.4981, -0.1552, -0.0102, 0.0223],
               reffreq= '1.47GHz',
               polindex= [0.078],
               polangle=[-0.16755],
               standard='manual',
               scalebychan=True,
               usescratch=True,**args)
         elif field == 'J1331+3030':
            print(f"Setting flux for Polarization calibrator {field} using Manual")
            setjy(
               fluxdensity= [14.7172,0.0,0.0,0.0], 
               spix= [-0.4507, -0.1798, 0.0357],
               reffreq= '1.47GHz',
               polindex= [0.098],
               polangle=[0.575959],
               standard='manual',
               scalebychan=True,
               usescratch=True,**args)
         elif field == 'J1939-6342':
            print(f"Setting flux for Primary calibrator {field} using Manual")
            setjy(
               standard='Stevens-Reynolds 2016',
               scalebychan=True,
               usescratch=True,**args)
         elif field == '0408-65':
            print(f"Setting flux for Primary calibrator {field} using Manual")
            setjy(
               fluxdensity=[15.523235461976729],
               spix= [-1.134509596601266, -0.30895512817637644, 0.0861000700373343, 2.5665707746471283e-07],
               reffreq= '1.40GHz',
               polindex= [],
               polangle=[],
               standard='manual',
               scalebychan=True,
               usescratch=True,**args)
         else:
            print(f"Setting flux for Secondary calibrator field {field} using manual")
            setjy(
               standard='manual',
               scalebychan=True,
               usescratch=True,**args)
      inputs:
         ms:
            dtype: MS
         field:
            dtype: str   
               
   
  
 
 

  
   
